<h2> <%=@article.title%> </h2>
<h4> original creator: <%=link_to(@article.author.username, @article.author)%>
<div = "article-body" >
  <% if @last_revision %>
    <p> <%= body_with_links_and_images(@last_revision)%> </p>
  <% else %>
    <p> No revisions just yet. </p>
  <% end %>
</div>

