<div class="container">
  <%= link_to_if(logged_in?, "Create Revision") %>

  <% if current_user.role == "admin" %>
    <%= link_to 'Delete', @article, :method => :delete, :confirm => "Are you sure?" %>
  <% end %>
  <h2> <%=@article.title%> </h2>
  <hr>

</div>


