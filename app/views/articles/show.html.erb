<div class="container">
  <!-- new_article_revision_path(@article.id) -->
  <%= link_to_if(logged_in?, "Create Revision" ) { "Login to make revision" } %>

  <% if current_user && current_user.role == "admin" %>
    <%= link_to 'Delete', @article, :method => :delete, :confirm => "Are you sure?" %>
  <% end %>
  <h2> <%=@article.title%> </h2>
  <hr>

</div>


