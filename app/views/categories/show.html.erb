<h2><%= @category.name %></h2>

<ul id="categorys_articles" class = "no-bullets" >
<% @category.articles.each do |article| %>
    <% if article.status == 'published'%>
      <li><%= link_to article.title, article_path(article) %></li>
        <% if article.featured %>
          <%= button_to "Mark as featured", article_path(article),remote: true, method: :patch %>
        <% else %>
            <%= button_to "Clear featured", article_path(article),remote: true, method: :patch %>
        <% end %>
    <% elsif article.status == 'unpublished' && (current_user.role == 'admin' || current_user.role == 'superadmin') %>
      <li><%= link_to article.title, article_path(article) %></li>
      <%= button_to "Mark as featured", article_path(article),remote: true, method: :patch%>
    <% end %>
  <% end %>
</ul>

