<h2> Registration: </h2>

<%= form_for @user do |f| %>
  <%= f.text_field(:username, placeholder: "Username") %> <br>
  <%= f.password_field(:password, placeholder: "Password") %> <br>
  <%= submit_tag("Submit") %>
<% end %>
